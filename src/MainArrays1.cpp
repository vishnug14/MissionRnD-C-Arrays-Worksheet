#include "FunctionHeadersArrays1.h"
#include <stdio.h>
#include<conio.h>

int main(){

	//Test RemoveArraysDuplicates
	/*
	int arr[1] = {NULL};
	int len = removeArrayDuplicates(arr, 1);
	printf("%d", len);
	*/
	
	//Test Students Count
	/*
	int arr[7] = { 10, 20, 30, 30, 30 ,40,50};
	int lessCount, moreCount;
	studentsCount(arr, 7, 30, &lessCount, &moreCount);
	printf("%d %d", lessCount, moreCount);
	*/
	/*
	int arr2[3]={1,4,10};
	int *newarray=sortedArrayInsertNumber(arr2, 3, 3);
	printf("%d %d %d %d", newarray[0], newarray[1], newarray[2],newarray[3]);
	*/
	
	_getch();

	return 0;
}
